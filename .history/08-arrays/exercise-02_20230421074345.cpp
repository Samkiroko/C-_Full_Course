#include <iostream>

using namespace std;

int main()
{
  /*
  - Assume the collection is sorted
  - Iterate through the collection an if i == 0 continue
  - if given index is not greater than the last given index
  then sorted = false
  break
  */

  return 0;
}